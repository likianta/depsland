def main():
    print(
        '''
        Make sure the following tools are working:
        - ossutil (`ossutil version`)
        - poetry (`poetry about`)
        - vlang (`v version`)
        ''',
        ':r2'
    )
