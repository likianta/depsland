# run command: py build/install_requirements.py -h

# pip
# setuptools
wheel

argsense >= 0.4.3
lk-logger >= 5.4.6
lk-utils >= 2.5.0
# neoprint >= 0.1.0b0
oss2
pyyaml
rich
semver >= 3.0.0dev3
# textual

# gen-exe  # windows only
# pywin32  # windows only
